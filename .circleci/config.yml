orbs:
  python: circleci/python@2.1.1
  
jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - python/dist
workflows:
  example-workflow:
    jobs:
      - example-job